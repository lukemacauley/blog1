<%= simple_form_for(@post) do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.collection_radio_buttons :draft, [[true, 'Idea'], [false, 'Publish']], :first, :last %>
    <%= f.input :slug, placeholder: "Leave blank to automatically generate" %>

    <%= f.button :submit, 'Save' %>
<% end %>
